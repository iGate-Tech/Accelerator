version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=staging
      - PORT=5000

    volumes:
      # Mount .env file for environment variables
      - ./.env:/app/.env:ro
    networks:
      - accelerator-network

networks:
  accelerator-network:
    driver: bridge
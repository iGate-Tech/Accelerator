version: '3.8'

services:
  app:
    build: .
    expose:
      - "4000"
    environment:
      - NODE_ENV=production

    volumes:
      # Mount .env file for environment variables
      - ./.env:/app/.env:ro
    networks:
      - accelerator-network

networks:
  accelerator-network:
    driver: bridge
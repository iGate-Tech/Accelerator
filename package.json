{
  "name": "accelerator-db",
  "version": "1.0.0",
  "description": "Database setup and management for Accelerator",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "vitest",
    "test:unit": "vitest unit",
    "test:integration": "vitest integration",
    "test:e2e": "vitest e2e",
    "test:coverage": "vitest --coverage",
    "test:db": "vitest --run integration",
    "db:install": "npm run db:reset && npm run db:setup && npm run db:seed",
    "db:reset": "node scripts/db/reset.js",
    "db:reset:users": "psql \"$SUPABASE_DB_URL\" -f db/maintenance/reset-users.sql",
    "db:reset:tables": "psql \"$SUPABASE_DB_URL\" -f db/maintenance/reset-tables.sql",
    "db:reset:functions": "psql \"$SUPABASE_DB_URL\" -f db/maintenance/reset-functions.sql",
    "db:reset:triggers": "psql \"$SUPABASE_DB_URL\" -f db/maintenance/reset-triggers.sql",
    "db:reset:views": "psql \"$SUPABASE_DB_URL\" -f db/maintenance/reset-views.sql",
    "db:setup": "node scripts/db/setup.js",
    "db:seed": "node scripts/db/seed.js",
    "db:create-users": "node scripts/db/create-users.js",
    "db:backup": "node scripts/db/backup.js",
    "db:migrate": "node scripts/db/migrate.js",
    "db:dev:setup": "NODE_ENV=development npm run db:install",
    "db:test:setup": "NODE_ENV=test npm run db:install",
    "db:prod:backup": "NODE_ENV=production npm run db:backup"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.84.0",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.2.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "supabase": "^2.70.5",
    "uuid": "^13.0.0",
    "vitest": "^4.0.16"
  }
}

<div class="flex h-[calc(100vh-4rem)]">
    {{> sidebar projects=projects}}
    <main class="flex flex-col justify-center items-center p-4 sm:p-6 bg-background overflow-y-auto w-full md:ml-0">
        <div class="w-full max-w-6xl">
            {{>idea-creation}}

            <!-- Ideas Grid Section -->
            <div class="mt-8 sm:mt-12 mx-2 sm:mx-4">
                    <div class="columns-1 md:columns-2 lg:columns-3 column-gap-4 sm:column-gap-6 relative">
                        {{#each ideas}}
                        {{> idea-card .}}
                        {{/each}}
                    </div>
            </div>
        </div>
    </main>
</div>


